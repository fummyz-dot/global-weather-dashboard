# Frontend

React、TypeScript、Viteで構築したGlobal Weather Dashboardのフロントエンドです。

```bash
cp .env.example .env.local
npm install
npm run dev
```

通常は `http://localhost:5173` で起動します。バックエンドを
`http://localhost:8000` で起動してから利用してください。

```bash
npm run lint
npm test
npm run build
```
